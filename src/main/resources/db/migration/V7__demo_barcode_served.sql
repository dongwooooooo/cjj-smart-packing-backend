-- 입고 시연에서 이미 내준 바코드를 표시한다.
--
-- 화면 버튼이 바코드를 하나씩 받아가는데, "확정된 상품은 목록에서 빠진다" 로만 판단하면
-- 촬영 전에 버튼을 다시 누를 때 같은 바코드가 계속 나온다. 내준 시각을 남겨 진행 위치를
-- 서버가 들고 있게 한다. 리셋이 이 값을 비운다.
ALTER TABLE demo_product ADD COLUMN served_at TIMESTAMP;
